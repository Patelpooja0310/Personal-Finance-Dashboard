# =============================================================================
# .env.example — Copy this to .env and fill in your values
# python-dotenv will load these automatically
# =============================================================================

# MySQL connection
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=MySQL@Project123
DB_NAME=finance_dashboard